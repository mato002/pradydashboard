<div class="rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-10 text-center dark:border-slate-700 dark:bg-slate-900/40">
    <h3 class="text-sm font-semibold text-slate-900 dark:text-white">{{ __('Webhooks') }}</h3>
    <p class="mt-2 text-sm text-slate-500">{{ __('Incoming and outgoing webhook delivery logs will appear here once webhook endpoints are persisted.') }}</p>
    <p class="mt-4 text-xs text-slate-400">{{ __('No webhook deliveries recorded yet.') }}</p>
</div>
